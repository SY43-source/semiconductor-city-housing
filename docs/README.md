# Housing design simulator

`index.html` is a self-contained tool that works backwards from an age-inflow profile to a unit-size mix, room-count mix, tenure split, and the number of units needed per year. Parameters are calibrated on the Godeok observations described in the manuscript.

No server, no build step, no external libraries — open the file, or serve this directory with GitHub Pages.
